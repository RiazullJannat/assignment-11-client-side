import { styles } from "../../Styles";

const Processing = () => {
    return (
        <div className="h-screen min-w-full flex justify-center items-center">
            <h1 className={`${styles.heroHeadText} `}>Sorry! This page is in construction.</h1> 
        </div>
    );
};

export default Processing;